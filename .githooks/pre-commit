#!/bin/sh
# Pre-commit hook: run Deno checks before allowing commit
# Requires: Deno installed and accessible in PATH

# Format (check only)
deno fmt --check || exit 1

# Lint
deno lint || exit 1

# Tests
deno test || exit 1

exit 0
